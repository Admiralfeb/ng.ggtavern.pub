image: node:11

stages:
  - install
  - test
  - build
  - deploy

install-dependencies:
  stage: install
  cache:
    policy: push
    paths:
      - node_modules/
  script:
    - npm install
  only:
    changes:
      - package.json

lint:
  stage: test
  allow_failure: true
  cache:
    policy: pull
    paths:
      - node_modules/
  script:
    - npm run lint

build:
  stage: build
  cache:
    policy: pull
    paths:
      - node_modules/
  script:
    - npm run build
  artifacts:
    expire_in: 1 week
    paths:
      - dist/

ggtavern:
  stage: deploy
  cache:
    paths:
      - node_modules/
      - .firebase/
  only: 
    - develop
    - release
    - hotfix
    - master
  script: npm run deploy
  environment:
    name: production
    url: https://ggtavern.admiralfeb.net
  dependencies:
    - build